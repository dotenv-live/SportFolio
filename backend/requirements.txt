# ─────────────────────────────────────────────
# SportFolio Backend – Python Dependencies
# ─────────────────────────────────────────────

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic[email]>=2.10.0
pydantic-settings>=2.7.0

# MongoDB async driver
motor>=3.6.0
dnspython>=2.6.0

# Auth / JWT
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.0

# Rate limiting
slowapi>=0.1.9

# Background scheduler
APScheduler>=3.10.0

# AI / ML (placeholders – swap for real models in production)
numpy>=1.26.0
# xgboost>=2.0 – uncomment when training real models
# torch>=2.1  – uncomment when training real LSTM

# Dev / testing
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
